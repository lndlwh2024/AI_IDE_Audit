# 0.3.0a1 验证记录

日期：2026-09-09。平台：Windows，Python 3.12.14，MCP SDK 2.2.0，Codex CLI 0.153.4。此记录区分测试证据和产品完成度，不将测试数量解释为功能数量。

## 自动化回归

最终命令：`python -m pytest -q -p no:cacheprovider --basetemp=.venv/dev_test10 --junitxml=.ide_audit/release-tests.xml`。

结果：**79 passed，52.01 秒**。覆盖原测试及新增的真实 STDIO、只读角色、固定提交证据、篡改拒绝、线程/多进程账本、前后图谱、非 Python 声明冻结、重命名/删除/二进制、安装保留、迁移回滚、租约所有权和派发恢复等。

本地 JUnit、运行时数据和测试仓库位于忽略目录，不提交到公共源码。修复前基线为 54 passed / 7 failed。

## 安装包

通过 setuptools 构建 `ide_audit-0.3.0a1-py3-none-any.whl`，安装至独立目标目录，在非源码工作目录运行导入检查；确认实际导入来自安装目录、规则 YAML 可加载、三个模板存在、CLI 帮助和 MCP 服务可导入。

该检查共享当前环境依赖，不等于全部依赖从空白操作系统重新安装。后续 CI 应覆盖 Python 3.11+ 和目标平台。未向 PyPI 发布。

最终本地 wheel 的 SHA256：`b0b4963d1d2e658dfe3c9694bdc485624dcbb9641dc3a4719844dfe8d6dc9b87`。

GitHub Actions 的 Windows/Linux × Python 3.11/3.12 四组测试和构建均已通过：[运行 34292263659，源码提交 973f388](https://github.com/lndlwh2024/AI_IDE_Audit/actions/runs/34292263659)。这是独立远端环境的安装、测试与打包证据。该次运行提示旧 Actions 使用 Node 20，随后依据官方发布将 checkout 更新为 v7.0.1、setup-python 更新为 v7.0.0；最新工作流结果应按对应提交在 Actions 页面核对。

## 真实 Codex 基础闭环

全部数据为独立临时仓库中的合成数据，不使用本项目实际源码进行模型测试。

| 验证 | 实际结果 |
|:---|:---|
| 正例提交 `f63dc9f172d999ea859bd9e3b19407d58eaa6b52` | 需求只改 service.py 的值 1→2，返回合理，完整覆盖该文件 |
| 反例提交 `ac995ebc0abef1bc6af03d1a222654130e9e1c17` | 需求只改值 2→3，但提交新增 unrelated.txt；即使文件已申报，B 仍返回不合理并指出相关性为 false |
| 会话隔离和复用 | 会话 `01a08361-3685-7bd1-9b0c-9dd40fed5641`，两次审计使用同一独立 B，不依赖 A 开发记忆 |
| 桌面可见 | Codex 桌面任务列表返回该会话；后来设置简洁 IDE_Audit 标题，避免证据 JSON 作为标题 |
| 追问 | 关闭原客户端、重新连接后询问文件相关性，收到符合固定提交的说明 |
| 原轮次恢复 | 将临时测试任务状态模拟为 running，从 `thread/read` 读取服务端原轮次并恢复结构化裁决，没有重发审计 |
| 拒绝写入 | 请求将源码改为 99，B 明确拒绝；源码哈希、Git HEAD 和工作区 diff 未变化 |

受限进程的首轮测试超时、审批和额度曾阻塞联网重试；后续在批准的联网环境中完成上述验证，因此不再把该临时阻塞当作当前产品结论。

## 尚未由以上证据证明的事项

- 自动弹出/聚焦独立桌面窗口；列表可见仅证明会话可被桌面发现。
- 大型代码库、多语言语义自动解析、完整原 Skill 历史数据兼容与跨步骤恢复事务。
- 长时间掉线、审批请求变化、连续提交忙碌队列的完整实机矩阵。
- 系统级恶意写入绕过测试；当前只读证据是请求参数、工具角色和实际拒绝编辑行为。

正式发布继续遵循 HANDOVER 和 DEVELOPMENT_PLAN 的剩余验收门。
