# 0.3.0 验收记录

日期：2026-09-09。Python 3.12.14 / Windows，Codex CLI 0.153.4。

- 完整自动化回归：103 passed，59.36 秒。pytest 缓存目录权限产生 1 条警告，不影响断言；验收临时目录为 .venv/final-regressions-1。
- 原生 B 实机：同项目 2 次模型裁决（合理/不合理），同一个 B ID；归档后新建同项目 B2。见 HOST_COMPATIBILITY.md 中固定提交和真实轮次编号。
- 图谱：未变扫描不读源码、不重复 AST 解析；新增模块重新关联旧导入；全量与增量结果一致；人工 API 边和字段保留。
- 事务：账本已写/图谱未发时恢复，无重复事件；只修复本操作可验证的半条追加，不截断其他坏数据；游标不会跳过别人未读事件。
- 队列：恢复原轮次；确定失败最多一次自动重试；桌面待派发阻塞后续队列；领取幂等保护、忙碌不消费、拒绝错请求标识。
- 外部迁移：真实 NEWS 只读预检；临时副本 140 条事件、612 节点转换和回滚，原资产哈希不变，未修改 NEWS。
- 打包：统一插件清单和 Skill 校验；wheel 排除外部迁移；隔离安装核验版本与入口。最终附件以 dist/SHA256SUMS.txt 为准。

没有执行整个 Codex 桌面强退重启和永久删除聊天；不宣称全平台、全语言自动语义解析或系统级只读隔离。非 Python 语义由 A 维护，固定事实包区分自动物理证据与人工申报。
