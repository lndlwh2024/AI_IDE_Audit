# 0.4.1 验证与交付

日期：2026-09-11。以源码、测试、NEWS 接入回执及宿主计数为证据；真实 NEWS 下一次业务提交/B 裁决尚待用户验证。

最终本地全套：129 项测试通过。实际 NEWS 已安装 wheel 的新 MCP：29 个工具，enabled，未读事件 0，无更新响应 714 字符；参数错误能返回明确中文原因。插件与 Skill 官方校验均通过。

## 已完成

- Python BOM UTF-8/16/32、编码声明兼容；非活跃历史坏文件保留未验证语义并提示覆盖警告。测试确认该文件一旦本次改动仍使物理审计缺证，活跃坏文件仍阻断。
- B 名称规则已覆盖后端派发及原生桌面桥接，归档/删除后的序号逻辑保留；新 B 无旧消息重放。同项目连续 B 和归档替代的实机证据延续 0.3.0，本轮新增名称断言。
- A 阶段起止用量和 B 审计完成摘要；只取指定任务，校验根目录、项目、日志头及文件边界。旧 A 无 projectId 时仍核对目录和当前项目授权。统计失败显示未知，不阻断同步。
- 用量查询不读取完整任务历史，避免 thread not loaded；大日志首次本地扫描，随后从缓存偏移读取，尾部指纹校验、部分行忽略、计数回退不猜测。
- 图谱维护只返回摘要，账本分页；单条超大事件按 6000 字符顺序分段，全部阅读才允许确认；未变化源码缓存复用。正常工具错误通过 MCP ToolError 直接给出可处理原因，避免模型反复查插件源码。
- B 所需的需求、diff 和申报保留，图谱限定本次文件及一跳依赖；事实包 64000 字符上限，超出停止派发。5000 个无关节点的测试图谱可保持传输包小于 3000 字符，不删 diff，不静默截断。

## NEWS 实机结果

原 A 完成恢复，项目真实状态 enabled；会话 CODEX-MAIN_202609110708_A0001。外部修复脚本校验旧版本映射，继承同一 A 已读 139 行，再阅读并确认最后一条，当前游标 140。该脚本位于开发验证目录，没有进入插件或 wheel。

图谱从旧 612 节点刷新为 7768 节点/1480 边；不是把新节点全部发送给模型。相同范围旧扫描候选 58513 节点，本次筛除未跟踪的依赖缓存、临时构建及历史发布产物；Git 已跟踪目录不会因通用构建名称被忽略。再次预览 files_read=0、files_total=7768。图谱摘要约 491 字符，完整图谱约 2.24 MB 留在本地。

scratch/old_base_agent.py 有 UTF-16 BOM，解码后仍有历史语法损坏；迁移前已声明 non_active/archive_diagnostics。保留该源文件字节及旧语义，显示 1 条覆盖警告，不能声称该文件解析通过。

HEAD 仍为 2add420693d90b4304c33704c657d85fadd15e5c；安装前已有 8 个修改文件哈希不变，11 个原始资产哈希不变，旧 Skill 保持停用。没有 NEWS 业务提交、push 或部署。切换回执仍保留真实 B 验收待办。

## 用量结果与实际边界

0.4.0 失败同步时没有启动 B。旧 A 21 次用量记录的连续可观测段，加首个请求为 1,752,736 token，其中 1,391,488 为缓存输入；相对更早计数有回退，因此不能用旧累计直接相减，更不能映射成用户观察到的 30% 额度。

0.4.1 成功恢复的 initial_sync 区间：342,670 token，缓存输入 341,504，输出 250；结束时宿主当前累计 1,192,140。区间包含长 A 上下文与读取最后历史事件、确认调用，不含尚未结算的最终回复。这不是整个迁移任务的总费用，也不是每次普通提交的预期成本。两次宿主兼容排查另有消耗，不能把成功区间当成全部修复成本。

限制：长 A/B 历史仍随模型调用携带；保留同一 B 时无法承诺成本恒定。工具编排可将计量与相邻操作合并为一次模型交互，但涉及阅读理解/授权的步骤不可盲目确认。账户额度规则不是 token 等比换算，本次没有宣称配额降幅。后续用同规模真实提交观察 A/B 区间与缓存占比，再评估实际成本。

受限命令环境可能读到空 Codex 项目列表；宿主 MCP 或经授权的本地执行可正常读取。不移除项目校验。新技能和新 MCP 工具需新建任务或重启 Codex 后加载；已存在的旧服务不会因 wheel 升级自动热更新。

完整退出重启、所有宿主版本、信任 Hook 后自动询问、NEWS 下一次真实 B 审计仍未追加实测。不能把本次 enabled 等同全部端到端场景通过。
