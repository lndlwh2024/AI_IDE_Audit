# 0.4.8 验证


## 2026-09-13：0.4.8 B 载荷与报告交付根因修复

实测 NEWS 74953c7d 的原附件 47483 字符，事实包 43840；graph_analysis 34074，主要是全项目 796 个孤立节点。之前只裁剪四份图谱快照，漏掉全局分析列表。修复后完整桥接消息 11482 字符（约减少 76%），事实包 10148；需求原文 1644、真实 diff 438、三条账本事件 4312 不删改。分析只派发涉及本次文件的孤立节点/环及全局计数；完整封存数据不变。

真实 B 的日志明确表示旧高优先级守则要求最终仅 JSON，新派发消息不能可靠覆盖。原兼容解析仍可回收正确裁决，但没有正文不能视为完整报告交付。新 B 守则重写为 625 字符单一契约；旧 B 保留复用，裁决完成与 requires_b_panel 分离，A 必须将确定性四段报告文件打开在原 B 面板，不重发审计。原有报告只需本地重新排版。当前轮 B 计数按 audit_id 匹配，不再拿上一轮已完成计数替代。

完整输入预算改为 24000 字符，涵盖守则/契约/schema/证据；超限停止，不截断需求或 diff。状态查询去除重复 message/usage_before/previous_turn_ids；模型输入另存 jobs/<SHA>/b-inputs/<hash>.json 供审计，明确仅准备记录不是发送回执。get_audit_input_manifest 返回字段体积清单。

限制：旧 B 聊天中的既有高优先级指令不宣称已更新；本地报告生成与 B 面板交付避免依赖它。新输出协议自动测试不替代下一次宿主实测。载荷字符不是实际输入 token，复用 B 历史仍会影响每轮消耗。

验证结果：本地全套 152 项通过；桥接正文识别补充修改后相关 15 项通过。存在 3 条旧测试不完整报告夹具告警，未伪造报告交付成功。Codex personal 插件及 NEWS 独立环境升级 0.4.8；封存 input/result/verdict 哈希保持一致。真实已有裁决已重新排版为四部分，B 本次 395791、累计 497357；A 缺失记录仍明确保留缺失，共享区间不得相加。报告已指定在原 B 面板打开，未调用模型重审。旧审计用量来自原长载荷，新载荷估计不能视为该历史审计的事前预测。
