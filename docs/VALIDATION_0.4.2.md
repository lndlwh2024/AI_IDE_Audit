# 0.4.2 故障审计与验证

2026-09-12。NEWS 最新测试提交 db8aec5ffe221585a47224a4c76e54ce327141df，只新增 docs/IDE_AUDIT_SMOKE_TEST.md 一行，没有 push。

## 原因证据

原 A 此轮 19 次工具编排、20 次用量记录，宿主最终总数 2,585,778 token：输入 2,582,415，其中缓存输入 2,345,856，输出 3,363。首个请求输入 126,867，末个请求输入 141,233；最大单次工具输出约 9185 字符。大量输入主要是每轮携带长 A 上下文；重复流程、字段修正和 Hook 排查放大了轮次，不是新文档需要读取大量代码。账户配额不是总 token 的简单等比换算。

0.4.1 complete_sync_operation 无条件 preview 并提交图谱，即使说明文档也改图谱版本；本地 Python 不调用模型，但这违背无架构变化不更新图谱产物的要求。0.4.2 对新说明文档和语义无变化进行处理。

封存报告 analysis_status=complete、diagnostics=[]，一文件新增一行；账本漏报第 1 行，is_consistent=false。这是 B 的裁决材料，不是 B 启动阻断。原 CLI audit --notify 先打印完整报告，发生 GBK UnicodeEncodeError 后未执行 enqueue。只读核查发现此提交无队列、无 session.json、无 dispatch.json。派发包 39308 字符，未超过 64000 预算；未实际运行 B。

三个阶段显示 0 的记录，起止宿主计数完全相同；在工具调用内起止或统计延迟时无法归属当前请求，因此 0 不能证明这一阶段无消耗。已改为待结算/未知，同时保留原始观测差值供排查。

## 修复验证

新增回归覆盖：终端抛编码错误时已先入队；文档事件不改架构图谱三个产物或版本；无新增计数不报告零消耗；升级旧托管 Hook 保留原 Hook 链并可恢复。133 项本地回归通过，插件与 Skill 官方校验通过；NEWS 已安装 0.4.2，新 MCP 29 工具连接正常，enabled，无未读事件，摘要 768 字符；原始输入、封存报告、图谱和账本哈希不变，Hook 已升级。未创建 B 或重试队列。远端检查以发布结果为准。

MCP 事件结构显式化，已就绪服务报告 ready_sync_sessions；完整原文、文件范围、租约、记账和独立 B 仍保留。默认通知输出改为短摘要，完整证据继续封存在本地。

当前边界：未调用 NEWS A/B 重新测试以节省额度；没有同规模真实提交的额度降幅对照。长 A 历史仍有上下文成本，无法保证每次审计便宜。现有封存报告和第 1 行漏申报均保持不变；后续恢复同一提交，不补造证据、不自动重提。
