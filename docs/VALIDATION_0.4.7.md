# 0.4.7 验证


## 2026-09-13：0.4.7 Transport closed 根因与恢复入口

事实：NEWS 新启动独立 STDIO 的 0.4.6、33 个工具正常；原 A 的 MCP 连接关闭。Codex 宿主 PID 4564 仍从 2026-09-12 15:24 运行，未经历完整后台重启。上次停止旧开发 MCP 后，宿主未重连，原 A 缓存关闭的连接。独立 app-server reload 返回成功不能证明桌面既有连接更新。

此前验证失误：用磁盘/独立进程健康替代原 A 连接验收，又主动停止旧进程，使“旧协议”变成“连接关闭”。不再自动杀 MCP 或宿主进程，不把重启建议描述为已完成修复。

新增独立 CLI 恢复：runtime-status、phase-begin、phase-finish、prepare --usage-thread-id、desktop-claim、desktop-collect；复用同一授权、宿主身份、证据绑定和原生 B 防重复逻辑，每次命令启动当前磁盘版本。prepare 改为短摘要，避免全图谱输出。完整报告仍在 B，collect 给 A 仅报告路径。CLI 健康不代表桌面旧连接恢复；逐工具 MCP 日志不适用于 CLI 恢复，阶段/入队/B 统计继续保留。

NEWS 复用已暂存的一行文档和 v0142，不重复编辑记账；后续通过实际 A 验证新 CLI 入口。最终真实新提交和 B 审计由用户续接，未完成之前不能宣称端到端通过。

验证：148 项本地测试通过；3 个既有夹具非阻断报告警告保留。插件/Skill 校验通过，NEWS 安装包字节核对通过。已请求原 A 只运行 runtime-status 验证新通道，不操作提交。
