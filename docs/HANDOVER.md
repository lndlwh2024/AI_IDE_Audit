# IDE_Audit 当前交接与审计

更新：2026-09-10；当前交付实现 0.4.0。用户已确认本次方案、文档与开发。历史缺陷记录保留在后文，不代表仍未修复。

## 当前结论与功能审计清单

- 一个插件承接协同事件、诊断纠错、图谱、游标、租约、事务交接和固定提交证据；B 继续使用 Codex 大模型。
- 每项目明确授权后才接入，安装插件不自动启用。缺失/复制/损坏授权记录默认关闭，真实项目变化需要重新选择。
- A/B 可暂停与请求恢复，保留资产；只有 A 完成当前版本图谱/同步才能重新启用。旧需求与准备证据归档，旧积压任务不自动重放。
- 同项目原生 B、多轮复用、活动写入者桥接、归档递增编号保留；不以插件页面替代模型。
- B 真实 token 分项、轮次及已记录累计，去重、缺失未知。线程接口无统计时兼容读取指定 B 宿主日志；不包含 A 用量或费用。
- 启动/恢复 Hook 只检查项目状态，用户信任 Hook 后才可运行；明确同意项目后才安装业务入口。用户可手动开启，明确拒绝后留存、不重复询问。
- NEWS 单向切换工具独立 ZIP 交付，原资产保留，A 与真实 B 验证后才标记完成，无新旧双写和迁移回滚流程。

## 验证和边界

117 项本地测试覆盖原有闭环及新增控制/用量/Hook/外部切换。原生同项目两轮模型裁决、归档替代为 0.3.0 已有实机证据；本次追加真实 B 用量观测和发布 Hook 命令验证。详见 [0.4.0 验证](VALIDATION_0.4.0.md)。

完整桌面退出重启、多 Codex 版本、用户信任后的全流程自动弹询未实测。现实现依赖 CLI 0.153.4 实验项目协议；日志统计格式可能改变，失败显示未知。领取到原生发送存在跨系统间隙，不能保证自动恰好一次；中断必须核对原 B。暂停不能撤销已发送的宿主模型请求，原生在途轮次可能完成收尾。B 控制和用量工具可写元数据，不能再笼统声称整个审计 MCP 绝对只读；其不提供源码/图谱维护工具。

真实 NEWS 未切换，当前有未提交文件；已只读预检 140 事件、612 节点及 11 资产。外部快速切换流程见 [工具说明](../scripts/README.md)，由 NEWS 的 A 更新并完成实际开发提交验收，不由 B 扫描或本项目强行提交 NEWS 工作区。

当前版本、最终 CI 和公开附件以 GitHub 对应提交与 Release 实际结果为准。公共插件市场按用户要求本次不发布。

---

## 修复前审计记录（2026-09-08，历史基线）

> 审计更新：2026-09-08
> 产品需求基准：`docs/PRODUCT_DESIGN_V2.0.md` 当前工作区版本；V1.0 仅用于历史对照。
> 代码基准：本地 `main` / `c6b6abf`，加上审计开始时已存在的未提交修改。
> 包声明版本：`0.2.0`，不代表该版本已发布或通过验收。
> 总体结论：**已有核心模块和部分可用函数，但一体化插件尚未交付完成；当前不能通过 V2.0 验收。**

## 1. 本次审计结论

旧交接文档的“阶段一至四全部完成”“100% 完整融合”“仅 MCP 适配存在问题”应撤回。真实情况是：CLI 入口无法启动，MCP 服务导入失败，安装器和 B 窗口管理仍含模拟实现；即使绕过这些入口，Diff、需求归档、图谱和记账核验仍存在结果正确性问题。

已有 54 个测试通过，证明部分模型校验、单函数操作和基础流程符合现有断言，**不能证明 54 项产品能力完整可用**。阶段三、四尤其缺少真实协议和用户操作闭环的测试证据。

V2.0 的目标保持不变：将 dual-agent-sync 的完整能力纳入 IDE_Audit，一个产品、一个安装入口、统一数据目录、统一开发守则；最终不再要求独立安装或运行 dual-agent-sync。当前保留迁移回退材料是交付措施，不是长期保留两个插件的架构方案。

建议下一轮先做 Codex 真实接入可行性验证、修复入口和审计事实链，再补齐协同能力与双窗口闭环。详细方案、依赖顺序和验收门见 [后续开发方案与计划](DEVELOPMENT_PLAN.md)。

## 2. 审计范围与证据边界

- 阅读了 `docs/` 内现有三份文档：V1.0、V2.0、原 HANDOVER，并核对 AGENTS、README、打包配置、关键实现和现有测试。
- 使用隔离环境运行现有 61 个测试；另在新建实验仓库中复核入口、Diff、需求重审、图谱、账本及并发问题，构建并检查 wheel 内容。
- 本次是现状审计与文档更新，没有修复生产代码、安装项目 Hook、修改用户 Codex 配置、调用模型执行 B 窗口任务、迁移实际项目或推送 GitHub。
- 未获得可锁定版本的原 dual-agent-sync 完整 Skill、配套资源和真实历史数据样本。不能认定逐项功能等价或历史资产无损兼容；下一阶段必须补上原始能力基线。
- 未验证桌面 B 窗口出现、持续追问、重启恢复和最终裁决落盘；源码中的模拟调用不能替代这些证据。
- Git 远程状态仅依据本地 `origin/main` 引用，未执行 fetch/push；旧文档将推送挂起归因于 GCM 的说法缺少本次诊断证据。

### 2.1 审计开始时工作区状态

| 状态 | 文件 |
|:---|:---|
| 已修改、未提交 | `archguard/core/report_generator.py`、`archguard/mcp/server.py`、`docs/PRODUCT_DESIGN_V2.0.md`、`templates/audit_report.md.j2` |
| 未跟踪 | `docs/HANDOVER.md`、`scripts/migrate_ai_sync_to_ide_audit.py`、`tests/test_mcp_server.py` |
| 本地分支 | `main`，`c6b6abf`，相对本地远程跟踪引用显示 ahead 1 |

因此 `c6b6abf` 本身并不包含本次看到的全部交接材料与修改。后续修复前需要分别归属和审查这些既有变更，不能将它们当作已发布基线或无主代码覆盖。

## 3. 各阶段真实完成情况

| V2.0 阶段 | 已有实现 | 缺口与限制 | 本次评估 |
|:---|:---|:---|:---|
| 一：物理事实引擎 | 提交及文件类型识别、规则加载、路径信号、需求存储、报告渲染、结果保存 | 普通提交行数失真，无 patch/hunk 数据；内容规则实际匹配路径；重审需求失配 | 部分实现，需要修复正确性 |
| 二：内生协同与图谱/账本核验 | 17 个 Pydantic 模型、账本/游标/锁函数、基础 Python AST 扫描、图集合比较与文件集合核验 | 原 Skill 等价性未验证；锁未接入业务事务；图谱增量及依赖解析不完整；没有提交级输入绑定和行号核验 | 部分实现，未完成完整融合 |
| 三：MCP 与 Codex 双窗口 | 8 个工具函数声明、服务骨架、双端提示词模板、会话管理类 | MCP 无法导入；安装及会话仍为模拟；A/B 工具未隔离；没有可执行的 B 闭环 | 骨架阶段，未交付 |
| 四：CLI、集成、迁移、发布 | 5 个 CLI 命令文件、Hook 脚本、迁移脚本、可构建 wheel | CLI 引用不存在的接口；缺少端到端验证；迁移覆盖冲突未解决；wheel 缺规则和模板 | 骨架/初步实现，未交付 |

“有代码文件”“单元测试通过”“产品能力验收通过”必须分开登记，不再用一个“已完成”概括。

## 4. 问题清单与证据

优先级用于后续排期：P0 表示阻断基本使用或使审计关键事实失真，P1 表示完整融合及可靠交付的必修项。以下均是待修问题，本次没有将其标为已解决。

### H01 · P0 · CLI 命令矩阵整体无法启动

**证据**：`archguard/adapters/codex/installer.py:5` 引用不存在的 `get_ide_audit_dir`，配置模块实际提供 `get_audit_root`。`cli/main.py` 在启动时导入全部子命令，因此连 `archguard --help` 都失败。

进一步直接导入子命令，`commands/audit.py:3`、`graph.py:3`、`history.py:3` 分别引用不存在的 `AuditEngine`、`GraphAnalyzer`、`LedgerAnalyzer`。实际对应模块采用函数式接口，历史命令还误将操作账本作为审计报告历史。

**影响**：install/audit/commit/graph/history 均不能按文档使用。修复一个导入错误并不能让整个命令矩阵恢复。

### H02 · P0 · MCP 导入失败，备用服务也不是完整协议实现

**证据**：`archguard/mcp/server.py:79–83` 使用底层 `mcp.server.Server`，却调用其不存在的 `@app.tool`。本次安装 MCP 2.2.0，复现全部 7 个 MCP 测试失败，异常为 `AttributeError: 'Server' object has no attribute 'tool'`。

备用 `FallbackMCPServer` 仅处理 `tools/list` 和 `tools/call`，没有初始化握手处理，工具列表缺少输入 schema；当前测试只检查内部字典和装饰器，未进行真实 STDIO 客户端握手。不能沿用旧文档“直接改用备用服务即可”的建议。

此外仅有 `run_mcp_server()` 函数，没有独立 MCP 控制台入口或 `__main__` 启动模块，安装器也没有配置启动命令。应采用选定版本官方 SDK 的完整服务方式，测试公开协议，停止依赖私有 `app.tools` 结构。

### H03 · P0 · 安装和 B 窗口调用仍为模拟，产品闭环不存在

**证据**：`installer.py:25–33` 只打印配置更新和 Skill 创建日志，没有注册 MCP、注入守则或初始化全部资产。Hook 安装返回值未被安装器检查。

`session_manager.py:16–51` 明写“假设的 API 地址”和“模拟 API 调用”：固定返回 `new_thread_id_123`，恢复及发任务直接返回 `True`，实际 HTTP 调用被注释掉；该模块自身也受 H01 的缺失路径函数阻断。没有生产调用方把 `CodexSessionManager` 接到 Hook 或审计流程。

**影响**：不存在已证明可用的 B 会话创建/恢复/发送/完成/答疑链路，也没有可验证的裁决结果持久化。不能将“返回成功”当作 IDE 中已经出现审计窗口。

### H04 · P0 · Diff 事实不完整，内容规则漏检

**证据**：`core/diff_analyzer.py:152–158` 调用 `parent.diff(current)` 获取文件差异，后续从未生成的 patch 内容计算增删行数。`FileChange` 没有 `diff_content` 字段，但 MCP `get_file_diff` 访问此字段。

**复现**：普通提交将 `service.py` 的一行改成三行，Git `--numstat` 为 `3 / 1`，审计报告却为 `0 / 0`。提取原 `get_file_diff` 函数体执行，返回缺少 `diff_content` 的异常文本。

`architecture_detector.py:138–175` 将 `content_patterns` 匹配到文件路径，未匹配改动内容。相同提交在普通文件中加入 `import asyncio` 和 `CREATE TABLE`，架构信号仍为空。

**影响**：九大维度只是规则定义已存在，不等于九大维度检测完整实现；B 缺少逐文件核验与行号出入所需的原始证据。

### H05 · P0 · 审计与读取混用，重复审计会丢失或串用需求

**证据**：MCP `audit_changes` 直接调用 `run_audit`；后者读取当前 pending 并调用 `archive_and_clear`，随后重写 latest/history。没有以 commit 关联的不可变输入读取流程。`get_prompts` 也只读取当前 pending。

**复现**：同一 commit 第一次审计包含 1 条需求，第二次变为 0 条；之后记录一条“下一次尚未提交的需求”，再审计旧 commit，该需求被归到旧 commit 并清空。

**影响**：Hook 先审计、B 再调用工具时，B 可能拿不到本轮需求；快速连续开发或用户追问可能消费下一轮需求。账本和图谱同样读取工作区当前文件，未证明它们属于被审计的提交。

`prompt_store._write_json`、`engine._write_json` 实际直接覆盖写入，并不是注释宣称的原子替换。归档清空先于结果保存，且缺少整体事务和并发保护，需要一并修复。

### H06 · P0 · 图谱同步工具未完成初始化或增量扫描

**证据**：`sync/graph.py:24–29` 在无图谱时返回一个空 `CodeGraph` 对象，该对象为真值；MCP `sync_graph` 的 `if not graph` 初始化分支不会被触发。后续只是把刚读出的同一张图再写回，没有扫描源码变化。

`server.py:111` 传入 `trigger_version=None`，而 `GraphChangelogEntry.trigger_version` 要求字符串。图文件先写入，再因变更日志模型校验失败而报错。

**复现方式说明**：由于整个 MCP 模块已被 H02 阻断，本次单独提取原函数体，去掉注册装饰器后检查其下游行为；这不是协议验收。新项目有 `x.py`，调用结果为校验错误，图谱节点数仍为 0。

### H07 · P0 · 图谱基线、边标识及“违规”定义不正确

**证据与复现**：

- `engine.py:137` 没有传入旧图；`graph_analyzer.py:146–147` 默认为空图。只修改 README 的提交也把既存 `a.py`、`b.py` 报为新增节点。
- AST 产物使用文件路径作为节点键，例如 `a.py`，却把导入名 `b` 作为边终点。真实 `a.py → b.py → a.py` 相互导入被记录成 `a.py → b`、`b.py → a`，循环检测结果为空。
- `ImportFrom` 没有完整解析相对级别、包和导入成员；节点的 `module` 等于各自文件路径，缺少真正的模块划分。没有声明支持 Python 之外语言的解析能力。
- `check_cross_module_dependencies` 只要源/目标模块不同就列为违规，未结合允许依赖或分层规则。跨模块关系本身不能直接证明越权。
- 扫描排除规则检查绝对路径部件：实验项目只要位于名为 `.ide_audit` 的祖先目录下就被整体跳过；实际 `.venv` 目录却不在其排除名单中。

**影响**：既可能把历史结构误当本次变更，也可能漏掉真实环路。手工构造的理想图测试通过，不能证明 AST 生成图与分析器联动正确。

### H08 · P0 · 账本未限定本次提交，行号核验是占位

**证据**：`engine.py:159–163` 读取账本全部行，没有使用 `git.base_commit/head_commit`、提交边界或事件区间筛选。`ledger_analyzer.py:52–75` 的行范围逻辑保留 TODO 和 pass，始终返回空偏差列表。

**复现**：只提供上一次提交对 `main.py` 的申报，本次再次修改该文件且没有新申报，核验仍得到 `is_consistent=True`、评分 1.0。相反，历史中本次没有改动的文件也会被混入“虚报”集合。

另给出实际第一行变动的 patch，却申报 `999-999`，核验仍返回完全一致。该结果不能用于证明本次无未声明改动或无行号偏差。

### H09 · P1 · 锁、游标、流水和命名工具没有形成完整协同事务

**证据**：`LedgerManager.append_event` 的“读最新版本 → 分配版本 → 追加”没有锁。`CursorManager.update_cursor`、`GraphManager.update_graph` 将加锁责任留给调用者，但 MCP 没有实际组织锁事务。`AuditLogManager`、会话命名和未读游标流程没有接入 A 窗口工具调用链。

**复现**：在两个调用者都读取完最新版本后同时追加，两条账本事件均为 `v0001`。这是对真实竞争窗口的受控复现，不是生产压力测试。

因此旧文档“版本严格自增”“六步 RMW 已完成”“自动生成流水与状态快照”的描述不成立。还需核验持锁者身份、过期清理竞争、路径约束、游标回退和异常恢复。

### H10 · P0 · B 只读边界及证据不足状态没有落实

**证据**：MCP 将 A 写工具和 B 读工具注册到同一工具集合，没有角色授权边界；B 查询图谱/账本会实例化带目录创建副作用的管理器，`audit_changes` 更会消费需求并写入结果。会话管理器没有设置真实只读沙箱。

图谱和账本损坏时，引擎只写日志并返回空字典；报告模板会省略该节。实验中账本为无效 JSON，报告没有结构化的错误/完整性字段，需求流程仍继续归档。

**影响**：现状没有证明“B 查询无副作用”，也不能让 B 明确区分“没有变化”和“没有审计到”。这里的角色边界是 V2.0 交付要求，不等于断言当前已经发生越权写入。

### H11 · P1 · B 模板偏离双尺度裁决要求

**证据**：`templates/skill_codex_audit.md:9–11` 将宏观判断写成“是否符合现有架构图谱”，微观判断写成“被修改的文件之间是否强关联”。

正确基准应是**用户本轮原始需求**：用户明确授权时可以合理修改现有架构；多个文件彼此相关，也可能整体与需求无关。模板还缺少逐文件需求证据、结构化裁决资源清单、证据不足处理，以及完成报告后持续答疑的明确协议。

### H12 · P1 · Hook 和提交兜底不可靠

**证据**：`adapters/git_hook.py` 直接覆盖 `.git/hooks/post-commit`，卸载时直接删除，没有管理自己的标记区块或保留既有 Hook；假定 `.git` 是目录，未处理 worktree 的 gitdir 文件和自定义 hooksPath。Hook 仅调用当前已坏的 `archguard audit`，未绑定解释器和目标提交，也未对接 B。

`cli/commands/commit.py` 只运行 `git commit`，没有实现文档承诺的自动暂存或无 Hook 兜底；若干 CLI 捕获错误后只输出文本，缺少可靠非零退出状态。

**影响**：不能声称安装或提交后必然触发成功。post-commit 发生在提交之后，审计“不合理”只能报告/提示，当前设计并不阻止该 commit 已经落地。

### H13 · P1 · 迁移脚本不能承诺无损迁移或原 Skill 已下线

**证据**：`scripts/migrate_ai_sync_to_ide_audit.py` 遇到 `.ai-sync.bak` 会递归删除旧备份；向已存在的 `.ide_audit` 用合并复制覆盖同名文件。没有 schema/版本校验、内容哈希清单、冲突保护、事务回滚或旧规范引用处理。

当前只能认定“有目录复制及 dry-run 分支”。不能认定资产无损、可恢复，尤其不能直接用于已在写入的新目录。实际迁移及破坏性分支本次未执行。

### H14 · P0 · wheel 缺少运行必需资源

**证据**：按当前 `pyproject.toml` 成功构建 `ide_audit-0.2.0-py3-none-any.whl`，检查归档内容：无 YAML 文件、无模板文件，只有 `archguard` 一个控制台入口。

`rules/loader.py` 要求读取包内 `default_rules.yaml`；`report_generator.py` 依赖源码仓库根下的 templates。前者在缺资源安装中会阻断引擎，后者可能降级输出，但不代表安装包完整。源码目录的 editable 测试无法替代 wheel 安装验收。

## 5. 测试复核

### 5.1 环境与基线结果

本次在项目 `.venv` 安装当前 `pyproject.toml` 声明的依赖范围，没有修改依赖声明。主要版本：Windows、Python 3.12.14、pytest 9.1.1、GitPython 3.1.62、Pydantic 2.13.5、MCP 2.2.0、Click 8.5.0。它们是本次可复现环境，不是兼容性范围已经验证的承诺；Python 3.11 和旧 MCP 版本未做矩阵测试。

| 测试模块 | 通过 | 失败 |
|:---|---:|---:|
| architecture_detector | 11 | 0 |
| diff_analyzer | 6 | 0 |
| engine | 4 | 0 |
| graph_analyzer | 4 | 0 |
| ledger_analyzer | 5 | 0 |
| prompt_store | 6 | 0 |
| sync_cursor | 3 | 0 |
| sync_graph | 3 | 0 |
| sync_ledger | 4 | 0 |
| sync_lock | 3 | 0 |
| sync_schemas | 5 | 0 |
| mcp_server | 0 | 7 |
| **合计** | **54** | **7** |

标准临时项目路径下的复测结果：**61 项，54 passed / 7 failed，17.62 秒**。不能以排除 MCP 测试后的“核心 100%”替代全量结果。

首次将临时项目放在 `.ide_audit/` 下时得到 53 passed / 8 failed，额外失败是 H07 的绝对路径排除问题；改到没有该祖先目录名的实验路径后得到上述 54/7。两个结果均保留，不能把首次差异隐藏成一次全绿运行。

### 5.2 原测试没有覆盖的关键事实

- Diff 测试校验文件名和类型，但没有断言普通提交的精确增删行数和 patch。
- 图谱分析测试手写理想节点/边，未使用实际 AST 扫描输出验证环路、相对导入和本次基线。
- 账本分析测试使用简化对象，只测文件集合，没有提交区间或行范围的有效性断言。
- 引擎测试的“端到端”是临时 Git 仓库到 `run_audit` 函数，不包含安装、Hook、CLI、真实 MCP、Codex B 或答疑。
- MCP 测试检查内部结构，不等于客户端协议通信测试。
- 没有覆盖实际安装器、B 会话、迁移冲突、wheel 运行资源、并发业务事务的验收测试。

### 5.3 本次额外复核结果

| 场景 | 期望 | 实际 |
|:---|:---|:---|
| CLI 帮助 | 正常展示命令 | 缺失路径函数导致导入失败 |
| 普通提交 +3/-1 | 行数及 patch 与 Git 一致 | 0/0，无 patch 字段 |
| 普通文件增加异步导入及建表语句 | 对应架构信号 | 空信号 |
| 同一提交重审 | 保留本轮需求 | 从 1 条变为 0 条 |
| 新需求尚未 commit，重审旧 commit | 不消费新需求 | 新需求归入旧 commit |
| 文档提交，代码图不变 | 无新增代码节点 | 既存节点全部报新增 |
| a/b 相互导入 | 识别环路 | 空环路列表 |
| 仅有历史文件申报 | 本次仍应未声明 | 完全一致，评分 1.0 |
| 声明第 999 行、实际改第 1 行 | 行范围偏差 | 完全一致，评分 1.0 |
| 两写入者竞争版本号 | 唯一递增 | 两个 v0001 |
| 损坏的账本 | 明确证据缺失/错误状态 | 核验字段为空字典 |
| wheel 资源 | 包含规则和模板 | 均缺失 |

### 5.4 复测方法与本地材料

在项目根目录执行；临时目录每次使用新名字，避免清理已有实验材料：

```powershell
.\.venv\Scripts\python.exe -X utf8 -m pytest -q --tb=short -p no:cacheprovider --basetemp=.venv/review_pytest_new
.\.venv\Scripts\archguard.exe --help
.\.venv\Scripts\python.exe -m pip wheel . --no-deps --wheel-dir .ide_audit/review_wheels
```

本次本地辅助材料位于 `.ide_audit/review_20260908/`：原 HANDOVER 备份、两次 pytest XML、一次性复核脚本 `probe.py`、`probe_results.json` 及构建 wheel。该目录被 Git 忽略，不是已交付测试套件；下一轮应把相关场景转成正式回归测试。

## 6. 对旧 HANDOVER 的主要修订

| 旧表述 | 修订后的可验证表述 |
|:---|:---|
| 四阶段全部完成 | 四阶段均有代码，但三、四阶段仍含不可用入口和模拟逻辑 |
| dual-agent-sync 100% 无缝融合 | 部分能力已搬入 sync；调用链和能力等价验收未完成 |
| 唯一业务问题是 MCP 装饰器 | 已确认 H01–H14，涵盖入口、事实正确性、隔离与交付 |
| 三类锁保障并发，版本严格自增 | 有锁原语，但写入事务未接入，重复版本已复现 |
| 原子归档、图谱原子写入 | 实际直接覆盖写文件；缺少事务与失败恢复证据 |
| 八个标准工具、完整 CLI | 八个工具声明和五个命令文件，不代表可运行 |
| 无损迁移 | 有复制脚本，但存在覆盖备份和目标资产的风险 |
| B 创建、复用、持久答疑完成 | 固定模拟返回值；未联调 |
| v0.2.0 已交付、只待 push | 仅包版本声明，缺可用安装产物和验收证据 |
| push 挂起确定由 GCM 引起 | 旧记录的待核实诊断，本次没有复现或认证检查 |

原文中损坏的路径、转义字符及代码块已重新整理。V1.0 和 V2.0 原文件保持本次审计前内容，需求差异和建议在本交接及开发计划中显式说明。

## 7. 下一轮交接要求

1. 以 V2.0 为需求基准，补上原 dual-agent-sync 的版本、完整资源、真实资产样本和逐项等价验收清单。
2. 先验证 Codex 真实 B 会话及桌面可见性，再依据 H01–H14 修复入口、证据绑定与协同事务；不要先扩展到其他 IDE 或增加无关功能。
3. 每项修复交付“需求条目 → 修改范围 → 回归场景 → 测试结果 → 剩余限制”，使用真实失败样例做回归。
4. 区分业务代码只读、协同元数据写入、审计产物持久化三种边界。将持久化放到明确的编排/存储层，是下一轮建议方案，不能继续声称当前 core 完全无文件副作用。
5. 阶段一对未声明、范围偏差和跨模块依赖输出客观证据；不得从“未声明”直接推定主观瞒报。最终是否违反需求由 B 双尺度裁决。
6. 完整等价、真实双窗口、恢复和迁移验收通过后，随统一插件发版下线独立 Skill。当前不能发布“已完全替代”的承诺。

本次文档交付清单：现状结论已校正、关键缺陷有源码或运行证据、测试统计已复核、迁移/发布边界已澄清、后续开发计划已单独编制。生产问题仍待后续开发解决。
