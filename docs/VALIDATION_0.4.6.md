# 0.4.6 验证

- 145 项本地测试通过；3 项既有不完整夹具触发非阻断人读报告保存警告，裁决仍保留。
- 新增实际运行版本自报、升级后拒绝业务操作测试；插件和 Skill 校验通过。
- NEWS 安装 0.4.6；config/mcpServer/reload 返回成功，但进程检查仍见升级前的开发 MCP，不能据此宣称真实 A 已更新。已请求原 A 只读验证当前连接。
- 原测试未提交，因此未触发 B；保留暂存的一行文档及 v0142，不重复修改或记账。
