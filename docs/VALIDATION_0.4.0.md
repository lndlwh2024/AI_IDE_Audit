# 0.4.0 验证记录

日期：2026-09-10。基线为已发布 0.3.0，本次新增项目生命周期与用量功能。公共插件市场未发布，NEWS 未执行实际切换。

## 自动验证

- 完整回归：117 项，Python 3.12.14 / Windows。涵盖原有事实链与模型裁决协议、授权缺失/拒绝/异地复制/损坏、A 恢复失败、暂停保留队列、旧需求归档、B 权限工具集合、用量重复/回退/缺失/跨来源、外部单向切换等待验证。
- MCP 使用真实 STDIO 协议测试，项目目录服务在测试中显式替身，不能视为宿主真实项目注册测试。
- 发布 Hook 命令经 Windows 实际 shell 执行，安装路径含空格；正确读取 PLUGIN_ROOT，未授权项目不创建数据目录。
- 插件与 Skill 通过官方校验脚本；构建 wheel、插件 ZIP、外部工具 ZIP 和 SHA256SUMS。wheel 与插件不含 NEWS 迁移业务入口。

## 真实宿主观测

已有 B：01a086dc-4bce-7d42-a748-7611881bd46b；项目 H:/AIcode/Antigravity/AI_IDE_Audit。只读取指定任务，不执行新模型轮次。

account/usage/read 未返回指定线程用量，兼容读取 thread/read 返回的宿主日志并核验任务 ID、cwd、Codex 数据目录和日志头。结果来源 codex-rollout-token_count：输入 32380、输出 5、缓存输入 18048、推理输出 0、总 token 32385。缓存已含在输入中，总量不再加缓存；这只是既有 READY 测试 B 的观测数，不代表常规审计成本。轮次为 01a086e2-65d3-7713-a179-0e7c0b3ed0e0。

同项目双轮真实裁决、原生活动写入者桥接、归档后新 B2 的实机证据继承 [0.3.0 验证](VALIDATION_0.3.0.md)，不重复消耗模型额度来制造相同测试。

## 外部 NEWS

对 H:/AIcode/Trae/news 只读预检：140 条事件、612 个节点、11 个原始资产文件。单向切换的合成项目测试验证原资产不变、旧入口保留归档、未完成 A/B 验收不能 finalize。真实 NEWS 仍有未提交业务改动，本次没有修改或提交它们。

## 未实测与兼容边界

未关闭整个 Codex 桌面测试重启；未永久删除用户聊天；未替用户信任新 Hook，因此未把脚本验证宣称为桌面信任后自动询问闭环。信任要求和 PLUGIN_ROOT/commandWindows 依据 [官方 Hook 文档](https://learn.chatgpt.com/docs/hooks)。

原生消息领取与发送之间中断需核对原 B，不能盲目重发。暂停停止新工作，不能撤销已发送的宿主请求。B 日志格式不是稳定公开统计协议，变化或缺失显示未知；统计仅覆盖已记录 B，不含 A、费用或账户额度。超大项目、多宿主版本留作扩展环境验证。

远端 CI 与发布状态以对应提交及 GitHub Release 实际返回为准。
