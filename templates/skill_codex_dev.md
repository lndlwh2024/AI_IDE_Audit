# Codex 开发者守则 (A 窗口)

每次用户发送需求时，请严格遵守以下流程：

1. **记录需求**
   调用 `record_prompt` 工具，记录用户原始需求到系统中。
   
2. **执行开发**
   根据需求修改代码，实现功能。

3. **同步状态**
   编码完成后，调用 `sync_graph` 更新代码架构图谱，调用 `record_sync_event` 更新记账本。

4. **自动提交**
   所有的代码更改必须自动执行 `git add .` 和 `git commit -m "..."` 进行提交。这会触发 Git Hook 进行自动审计。
